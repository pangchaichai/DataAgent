# DataAgent 环境变量模板
# 复制为 .env 并填入真实值（.env 已加入 .gitignore，不会提交）

# DeepSeek API Key（探索式 SQL 生成外部兜底，需合规签字确认外发）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 企业内网 LLM（生产环境优先使用）
# ENTERPRISE_LLM_KEY=

# 开发模式强制走浏览器（不用 PyWebView）
# DATAAGENT_ENV=dev
