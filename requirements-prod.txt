# DataAgent Windows Production Dependencies
# Install (on Windows): pip install -r requirements-prod.txt

# Includes all of requirements-dev.txt, plus Windows-specific libs
-r requirements-dev.txt

# -- Windows-specific -----------------------------------------
pywebview==4.3.3       # native window
winotify==1.1.0        # Windows toast notifications
pyinstaller==6.6.0     # EXE packaging
psutil>=5.9.0
rank_bm25>=0.2.2
