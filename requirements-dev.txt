# DataAgent Dev Dependencies (Linux / macOS / Claude Code)
# Install: pip install -r requirements-dev.txt
#
# Notes:
#   This file only contains Linux/macOS dev dependencies, no Windows-specific libs.
#   For Windows production, use requirements-prod.txt
#
# Version policy:
#   Core data engine uses >= for cross-platform compatibility
#   (DuckDB/pandas/sqlglot verified with 1.x/3.x/30.x)
#   Web framework uses >= to keep flexibility

# -- Web Framework (dev/prod shared) --------------------------
flask>=3.0.3
flask-cors>=4.0.1

# -- Data Engine (dev/prod shared) ----------------------------
duckdb>=1.0.0          # verified 1.5.3
pandas>=2.2.0          # verified 3.0.3
openpyxl>=3.1.0

# -- Data Processing (dev/prod shared) ------------------------
chardet>=5.0.0         # verified 7.4.3
pyyaml>=6.0.1
jinja2>=3.1.4
python-docx>=1.1.0     # verified 1.2.0
requests>=2.32.0
sqlglot>=23.0.0        # verified 30.11.0 (API compat 23+)

# -- Scheduler (dev/prod shared) ------------------------------
schedule>=1.2.0

# -- Dev/Debug Helpers ----------------------------------------
pytest>=8.0.0
pytest-cov>=5.0.0

# -- Optional Features ----------------------------------------
psutil>=5.9.0              # process memory monitoring
rank_bm25>=0.2.2           # cross-session memory search (disabled by default)
pypdf>=3.0                 # PDF document parsing
duckduckgo-search>=6.0     # web search

# -- Native Window (optional) ---------------------------------
# macOS client mode (falls back to browser if not installed):
#   pip install pywebview
# Windows-specific (see requirements-prod.txt):
#   winotify==1.1.0    -> toast notifications
#   pyinstaller==6.6.0 -> EXE packaging
