# DataAgent 开发环境依赖（Linux / Mac / Claude Code 环境）
# 安装命令：pip install -r requirements-dev.txt
#
# 说明：
#   本文件只包含 Linux 开发环境所需依赖，不含 Windows 专属库
#   Windows 生产环境请使用 requirements-prod.txt

# ── Web 框架（开发/生产共用）──────────────────────────────
flask==3.0.3
flask-cors==4.0.1

# ── 数据引擎（开发/生产共用）──────────────────────────────
duckdb==0.10.3
pandas==2.2.2
openpyxl==3.1.2

# ── 数据处理（开发/生产共用）──────────────────────────────
chardet==5.2.0
pyyaml==6.0.1
jinja2==3.1.4
python-docx==1.1.2
requests==2.32.3
sqlglot==23.12.2

# ── 定时任务（开发/生产共用）──────────────────────────────
schedule==1.2.1

# ── 开发调试辅助 ──────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0

# ── 注意：以下库仅 Windows 可用，Linux 开发时跳过 ────────
# pywebview==4.3.3      → Windows 生产用，见 requirements-prod.txt
# winotify==1.1.3       → Windows 生产用，见 requirements-prod.txt
# pyinstaller==6.6.0    → Windows 生产用，见 requirements-prod.txt
psutil>=5.9.0
rank_bm25>=0.2.2
pypdf>=3.0           # PDF 文档解析（I-6）
duckduckgo-search>=6.0  # 联网搜索（I-6）
